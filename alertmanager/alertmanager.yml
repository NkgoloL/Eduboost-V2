route:
  receiver: slack-default
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 2h

receivers:
  - name: slack-default
    slack_configs:
      - api_url: ${ALERTMANAGER_SLACK_WEBHOOK}
        channel: "#eduboost-alerts"
        send_resolved: true
