"""API package for EduBoost.

V2 boundary note:
- `app/api/` may depend on `app/services/` and `app/domain/`.
- `app/api/` should not directly depend on `app/repositories/` once migration is complete.
"""
