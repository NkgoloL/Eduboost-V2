"""ETL pipeline integration package for Content Factory operations.

Only pipeline modules should live in this package. Optional MCP server wrappers
are isolated under ``tools/etl`` so app startup does not require MCP runtime
dependencies.
"""
