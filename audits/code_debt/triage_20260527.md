# Phase 1 — T120/T121 — Code Marker Triage

**Branch:** `remediation/phase0-phase1`
**Date:** 2026-05-27
**Tool:** `grep -rn "TODO\|FIXME\|HACK\|XXX" app/ --include="*.py"` + `grep -rn "^[[:space:]]*pass$" app/ --include="*.py"`

---

## TODO/FIXME markers (9 occurrences)

| # | File | Line | Marker text | Classification | Rationale / Issue link |
|---|------|------|-------------|---------------|----------------------|
| 1 | `app/modules/lessons/teacher_insight_mode.py` | 16 | `Implements TODO §6.4.3: "Add teacher insight mode..."` | `tracked-issue` | Feature backlog. Linked to Phase 2 learner-experience work. Not a release blocker. |
| 2 | `app/modules/lessons/adaptive_remediation.py` | 16 | `This implements TODO §6.4.2:` | `tracked-issue` | Feature backlog. Linked to Phase 2 learner-experience work. Not a release blocker. |
| 3 | `app/modules/lessons/parent_explanation_mode.py` | 18 | `Implements TODO §6.4.3: "Add parent explanation mode"` | `tracked-issue` | Feature backlog. Linked to Phase 2 learner-experience work. Not a release blocker. |
| 4 | `app/modules/lessons/budget_guardrails.py` | 151 | `# TODO: integrate with alertmanager or Prometheus Alertmanager webhook` | `tracked-issue` | Tracked under Phase 2 observability hardening (T233). Implementation exists for webhook wiring. |
| 5 | `app/api_v2_routers/auth_extended.py` | 560 | `# TODO: enqueue Celery task → export_user_data.delay(...)` | `tracked-issue` | POPIA Section 23 (data export). Implementation blocked by T110 (data export job). **Do not remove this TODO until T110 is merged.** |
| 6 | `app/api_v2_routers/auth_extended.py` | 587 | `# TODO: enqueue Celery task → schedule_account_deletion.delay(...)` | `tracked-issue` | POPIA Section 24 (right to erasure). Implementation blocked by T111 (account deletion job). **Do not remove this TODO until T111 is merged.** |
| 7 | `app/api_v2_routers/popia.py` | 106 | `# TODO: replace with real auth dependency that injects actor_id from JWT` | `tracked-issue` | Security / POPIA auditability. Tracked by T102 (replace POPIA router placeholder auth). **Do not remove until T102 is merged.** |
| 8 | `app/services/content_learner_read_service.py` | 382 | `# TODO: Implement legacy content fallback` | `tracked-issue` | Content delivery resilience. Tracked under Phase 2 content-fallback epic. Non-blocking for launch. |
| 9 | `app/services/content_learner_read_service.py` | 398 | `# TODO: Implement legacy content fallback` | `tracked-issue` | Same as #8 (second call site). Both call sites must be addressed together. |

---

## Bare `pass` statements (11 occurrences)

| # | File | Line | Context | Classification | Rationale / Action |
|---|------|------|---------|---------------|-------------------|
| 1 | `app/core/llm_gateway.py` | 124 | `class QuotaExceededError(Exception):` | `accepted-with-rationale` | Empty exception class. Idiomatic Python; no body needed. |
| 2 | `app/core/judiciary.py` | 34 | `class ConstitutionalViolation(Exception):` | `accepted-with-rationale` | Empty exception class. Idiomatic Python; no body needed. |
| 3 | `app/services/content_generation/prompt_payloads.py` | 38 | `class DiagnosticGenerationRequest(GenerationRequestBase):` | `accepted-with-rationale` | Empty subclass (marker / future fields). Acceptable for now. |
| 4 | `app/services/content_generation/prompt_payloads.py` | 42 | `class DiagnosticGenerationRequest(...)` (second `pass`) | `accepted-with-rationale` | Same as #3 (file-level scope, another empty subclass). |
| 5 | `app/services/content_generation_executor.py` | 47 | `class GenerationDisabledError(RuntimeError):` | `accepted-with-rationale` | Empty exception class. Idiomatic Python; no body needed. |
| 6 | `app/modules/progress/progress_timeline_service.py` | **27** (was 28 before fix) | `if subject and subject not in row.caps_ref:` | `fix-now` | **Fixed in this PR.** `pass` caused the filter to be a no-op; every row was grouped regardless of subject match. Changed to `continue`. See diff in this commit. |
| 7 | `app/core/llm_gateway.py` | 618 | `except json.JSONDecodeError:` | `fix-now` | **Silent swallow.** LLM returned malformed JSON and the error is swallowed without logging. This will mask production issues. Must log at minimum `warning` with request context. **Tracked: create issue for adding structured log + metrics counter on parse failure.** |
| 8 | `app/core/audit.py` | 57 | `# stdlib attributes structlog processors expect...` | `accepted-with-rationale` | Documented safe no-op: "logging must not break the protected action." The `pass` is intentional. |
| 9 | `app/modules/lessons/answer_key_verifier.py` | 127 | `except ValueError:` | `fix-now` | **Silent swallow.** Invalid float conversion is ignored silently, potentially propagating `None` or wrong values downstream. Should log at `warning` and return a structured error. |
| 10 | `app/services/lesson_authorization.py` | 118 | `# Constructor/signature/shape fallbacks are expected...` | `accepted-with-rationale` | Documented fallback pattern: constructor/shape mismatches are expected while repository surface settles. Data-layer failures must propagate as-is. Safe no-op. |
| 11 | `app/services/popia_consent_lifecycle_adapter.py` | 78 | `except Exception:` | `fix-now` | **Over-broad silent swallow.** Generic `Exception` catch around consent dict coercion is suppressed. Consent processing failures must be logged at `error` with learner_id and preserved in audit trail. |

---

## T121 — Remediation applied in this PR

### Fixed (`fix-now`) in this commit

1. **`app/modules/progress/progress_timeline_service.py:27`** — `pass` → `continue`
   - Impact: subject-filter parameter was non-functional; every topic was
     included regardless of the requested subject.
   - Fix: single-character change (`pass` → `continue`).
   - Regression risk: near-zero; the method only had one caller.

### Deferred `fix-now` items (tracked issues created)

The remaining `fix-now` items are deferred because they each need:
- Review of the caller to understand safe logging context (no PII leakage)
- Possible metrics counter addition
- Structured error contract (may need a new exception class)

| File | Line | Deferred to issue | Reason |
|---|---|---|---|
| `app/core/llm_gateway.py:618` | JSONDecodeError silent swallow | #T121-A | Needs structured log + metrics counter; may affect response format contract |
| `app/modules/lessons/answer_key_verifier.py:127` | ValueError silent swallow | #T121-B | Needs review of downstream consumer before changing return value |
| `app/services/popia_consent_lifecycle_adapter.py:78` | Generic Exception silent swallow | #T121-C | POPIA-sensitive; must include audit trail without leaking PII |

These are **not release-blocking** for the current launch scope because:
- The code paths they guard are error paths, not the happy path.
- The silent-swallow behaviour is pre-existing and was not introduced by
  Phase 0 remediation.
- Each has a documented, tracked follow-up issue.

### T121A — Add regression test for progress timeline subject filtering

The `pass -> continue` fix in `progress_timeline_service.py` changes the
behaviour of `get_subject_mastery_summary()` when the `subject` parameter is
provided. Before the fix, the subject filter was a no-op (all rows were
included). After the fix, only rows matching the subject are included.

**Acceptance criteria:**
- Unit test calls `get_subject_mastery_summary(learner_id, subject="Mathematics")`
  with a repository that returns rows for multiple subjects.
- Assert that the returned summary only contains the requested subject.
- Assert that the `topic_count` reflects only matching rows.

**Owner:** Engineering
**Priority:** P1 (should be added before the next release to prevent regression)

---

## Validation

```bash
# Confirm no new markers introduced by this PR
grep -rn "TODO\|FIXME\|HACK\|XXX" app/ --include="*.py" | wc -l
# Expected: 9 (unchanged)

# Confirm the fixed pass is gone
grep -n "^[[:space:]]*pass$" app/modules/progress/progress_timeline_service.py
# Expected: no output
```
