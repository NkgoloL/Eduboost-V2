# ─────────────────────────────────────────────────────────────────────────────
# docker/requirements.inference.txt
#
# [IMMEDIATE FIX 3] Inference-service-only dependencies.
#
# These packages are REMOVED from the main requirements.txt:
#   - torch>=2.3.1        (~2.3 GB CPU / much more with CUDA)
#   - transformers>=4.49.0
#   - huggingface-hub>=0.28.0
#
# The main API calls this service over HTTP; it does NOT import torch directly.
# ─────────────────────────────────────────────────────────────────────────────

# ── Inference runtime ──────────────────────────────────────────────────────────
torch>=2.3.1
transformers>=4.49.0
huggingface-hub>=0.28.0
sentencepiece>=0.2.1        # required by some tokenizers (flan-t5, mT5)
accelerate>=1.3.0           # device_map="auto" support
safetensors>=0.5.2          # fast / safe model loading

# ── Science stack (shared with main app but pinned independently here) ────────
numpy==1.26.4
scipy==1.13.1
scikit-learn==1.5.0
joblib==1.4.2

# ── Inference HTTP server ─────────────────────────────────────────────────────
fastapi==0.141.1
uvicorn[standard]>=0.30.0
pydantic==2.13.4
httpx==0.27.2

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv==1.2.2
structlog==24.4.0
tenacity==8.5.0
