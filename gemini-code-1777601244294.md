# AGENT MASTER EXECUTION MANIFEST: EduBoost V2

## 🤖 META-CONTEXT FOR EXECUTING AGENTS
**Read This Before Executing Any Tasks:**
You are working on **EduBoost V2**, a production-grade, AI-driven adaptive learning platform for South Africa. 
* **Architecture:** Strict Modular Monolith (Domain-Driven Design). 
* **Core Paradigm:** "Constitutional AI" (5 Pillars: Legislature, Executive, Judiciary, Fourth Estate, Ether).
* **Pedagogical Engine:** 2-Parameter Logistic (2PL) Item Response Theory (IRT) model.
* **Compliance:** Strict POPIA adherence (Zero-PII, immutable audit trails, Right to Erasure).
* **Constraints:** DO NOT introduce distributed task queues (Celery/RabbitMQ), microservices, or complex orchestration (k8s/Bicep) unless explicitly authorized. Use `BackgroundTasks` for async work. Optimize for AI cost-control and deterministic outputs.

**Current Project State:** `V2_BASELINE_IN_PROGRESS`

---

## 📂 PHASE 0: REPOSITORY BOOTSTRAP & FOUNDATION
*Objective: Establish the immutable structural rules of the V2 codebase.*

- [ ] **Task 0.1: Scaffold Domain-Driven Directory Structure**
  - **Action:** Create the exact folder hierarchy: `/app/api/`, `/app/services/`, `/app/repositories/`, `/app/domain/`, `/app/core/`.
  - **Success Criteria:** Directory tree exists with `__init__.py` files enforcing strict boundary imports (e.g., `/api/` cannot import directly from `/repositories/`).
- [ ] **Task 0.2: Implement Core Configurations (`/app/core/`)**
  - **Action:** Create `config.py` (Pydantic BaseSettings), `security.py` (JWT hashing logic), and `logging.py` (Structured JSON logging).
  - **Success Criteria:** App boots locally with environment variables loaded and structured logging active.
- [ ] **Task 0.3: Single-Node Dockerization**
  - **Action:** Write a streamlined `docker-compose.yml` and `Dockerfile`.
  - **Dependencies:** PostgreSQL 16, Redis 7 (Cache/Sessions only, NOT streams).
  - **Success Criteria:** `docker-compose up` yields a healthy FastAPI Swagger UI at `localhost:8000/docs` with DB/Redis connections established.

---

## 🏛️ PHASE 1: THE MODULAR MONOLITH (BOUNDARY ENFORCEMENT)
*Objective: Migrate and isolate the core operational logic without the legacy bloat.*

- [ ] **Task 1.1: Define the Domain Layer (`/app/domain/`)**
  - **Action:** Consolidate all SQLAlchemy ORM models and Pydantic schemas. 
  - **Entities:** `LearnerProfile`, `Guardian`, `AuditLog`, `Lesson`, `KnowledgeGap`.
  - **Success Criteria:** All models exist in the domain layer with zero dependency on FastAPI HTTP request objects or third-party LLM clients.
- [ ] **Task 1.2: Implement the Repository Pattern (`/app/repositories/`)**
  - **Action:** Abstract PostgreSQL database access. Create asynchronous CRUD operations.
  - **Success Criteria:** Services can query data via `LearnerRepository.get_by_id()` without writing raw SQLAlchemy statements in the service logic.
- [ ] **Task 1.3: Rebuild the 5-Pillar Services (`/app/services/`)**
  - **Action:** Port the business logic for the Legislature, Executive, Judiciary, Fourth Estate, and Ether.
  - **Constraint:** The *Fourth Estate* must now write directly to a PostgreSQL append-only audit table, completely removing the Redis Streams dependency.
  - **Success Criteria:** The business logic operates independently of API routes.

---

## 🧠 PHASE 2: AI GATEWAY HARDENING & COST CONTROL
*Objective: Stop event-loop starvation, prevent hallucinated formatting, and protect the API budget.*

- [ ] **Task 2.1: Fully Asynchronous LLM Inference**
  - **Action:** Implement `AsyncAnthropic` and asynchronous Groq clients within the Executive service.
  - **Success Criteria:** `await` is used for all LLM network calls. Load testing confirms the FastAPI worker pool is no longer starved during generation.
- [ ] **Task 2.2: Strict Schema Enforcement (Judiciary Upgrade)**
  - **Action:** Deprecate string-parsing. Implement Groq's `response_format` (JSON mode) and Claude's Tool Use mapping directly to Pydantic `TypeAdapter` models.
  - **Success Criteria:** `JSONDecodeError` runtime exceptions are mathematically impossible in the pipeline.
- [ ] **Task 2.3: AI Cost-Control Layer**
  - **Action:** Implement a Redis-backed semantic caching layer and daily token/request quotas per user ID.
  - **Success Criteria:** Identical requests (e.g., Grade 4, Fractions, "Tiferet" tone) serve from the cache in <50ms. Users exceeding daily quotas receive a `429 Too Many Requests`.

---

## 📐 PHASE 3: PEDAGOGICAL DEPTH & PSYCHOMETRICS
*Objective: Make the AI mathematically valid for educational diagnostics.*

- [ ] **Task 3.1: IRT Database Seeding (Alembic)**
  - **Action:** Create Alembic seed migrations containing a minimum of 500 calibrated IRT items (Grades R-7).
  - **Context:** Requires accurate $a$ (discrimination) and $b$ (difficulty) parameters for the equation: $P(\text{correct} \mid \theta) = \frac{1}{1 + \exp(-a(\theta - b))}$.
  - **Success Criteria:** The system can accurately trigger the "Gap-Probe Cascade" using real data, rather than the legacy 10-item sample.
- [ ] **Task 3.2: The "Ether" Cold-Start Fix**
  - **Action:** Build a 5-question visual onboarding micro-diagnostic.
  - **Success Criteria:** New learners are instantly categorized into an archetype (e.g., Keter, Yesod) on session 1, bypassing the historical 8-10 event lag.

---

## 🔒 PHASE 4: SECURITY & POPIA COMPLIANCE
*Objective: Zero-trust architecture for minor data.*

- [ ] **Task 4.1: RBAC & JWT Refresh Rotation**
  - **Action:** Implement secure authentication with Roles: `Student`, `Parent`, `Teacher`, `Admin`. Use short-lived Access Tokens and HTTP-only HttpOnly Refresh Tokens.
  - **Success Criteria:** Endpoints reject unauthorized access.
- [ ] **Task 4.2: Guardian Authentication for Section 24 (Right to Erasure)**
  - **Action:** Harden the `DELETE /api/v1/learners/{id}` endpoint.
  - **Success Criteria:** Deletion requests mandate a valid `Guardian` JWT. `BackgroundTasks` are successfully triggered to physically purge records across all tables within 30 days.

---

## 📈 PHASE 5: PRODUCTIZATION & REVENUE ENGINE
*Objective: Transform the technical architecture into a monetizable business.*

- [ ] **Task 5.1: Analytics & Telemetry Hooks**
  - **Action:** Integrate PostHog (or similar lightweight telemetry) at the API gateway level to track session lengths, lesson completions, and diagnostic drop-offs.
  - **Success Criteria:** "Product loop" metrics are actively tracking without exposing PII.
- [ ] **Task 5.2: The Parent Trust Dashboard**
  - **Action:** Expose specific `/api/v1/parents/` endpoints aggregating gap-probe results and AI-summarized progress.
  - **Success Criteria:** Data is structured specifically for frontend visualization (charts/graphs).
- [ ] **Task 5.3: Stripe Subscription Engine**
  - **Action:** Implement tiered billing logic (Free vs. Premium). Tie access to the AI Gateway quotas developed in Phase 2.
  - **Success Criteria:** Webhooks successfully process subscription states, granting Premium users unlimited adaptive AI access.