# Kubernetes Manifests

Kubernetes is post-MVP scope for EduBoost V2. The active deployment target is
the V2 modular monolith on Azure Container Apps via `docker-compose.v2.yml` and
the ACA/Bicep infrastructure.

Any manifest left in this directory is non-authoritative reference material and
must not be treated as the production deployment path.
