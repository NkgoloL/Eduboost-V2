# EduBoost True-State Remediation B01

**Slices:** TSR-0 and TSR-1  
**Workstream:** Release Gate Recovery

This archive is a transactional repository overlay. It contains implementation files, a self-checking application script, an independent verification wrapper, a cryptographic payload manifest, and exact operator commands.

Start with `IMPLEMENTATION_COMMANDS.md`. Do not copy individual files manually: use `apply_bundle.py` so prerequisites, backup, rollback, command evidence, manual controls, and next-bundle readiness are enforced.

Required human/external evidence controls: TSR-0.7, TSR-1.11.
